version: '3.5'

services:
  fixture.setup:
    build:
      context: .
      dockerfile: setup-fixtures.dockerfile
    image: alpine
    container_name: fixture.setup
    working_dir: /etc/demo/tests/scripts
    volumes:
      - ../..:/etc/demo
